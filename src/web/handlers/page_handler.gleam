import sqlight
import wisp.{type Request, type Response}

pub fn home(req: Request, db: sqlight.Connection) {
  todo
}

pub fn list_feeds(req: Request, db: sqlight.Connection) {
  todo
}

pub fn add_feed(req: Request, db: sqlight.Connection) {
  todo
}
